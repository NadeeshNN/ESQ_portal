import React from "react";
import { CCard, CCardBody, CCardHeader,} from "@coreui/react";

export default function QuotationMapRef() {
  return (
    <CCard
      //className="routehistoryGlass"
      style={{ width: "100vh", height: "65vh", color: "white" }}
    >
      <CCardHeader className="headerEQModal">Quotation Map Ref </CCardHeader>
      <CCardBody>Quotation Map Ref</CCardBody>
    </CCard>
  );
}
